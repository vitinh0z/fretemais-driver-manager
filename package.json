{
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend/driver-manager && mvnw.cmd spring-boot:run",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "axios": "^1.13.5",
    "clsx": "^2.1.1",
    "inside-cli": "^2.1.4",
    "tailwind-merge": "^3.4.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
